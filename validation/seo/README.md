# Live SEO sample — 2026-09-15

The script made 14 GET requests: sitemap index/children and four unique HTML pages (Chinese/English services and about pages). Sitemap traversal completed and contained 4,114 URL entries. Four seeds produced 72 pass checks, zero review/fail checks and four unknown checks: x-default is not a language code, so target-language compatibility is deliberately not judged.

[Full response metadata and extracted declarations](live-sample-2026-09-15.json). This is a four-page sample, not an audit of 4,114 pages. It does not establish body language quality, indexing, traffic, AI citations or business performance. The site's release state can change after observation.

Environment: macOS / Python 3.9.6, standard library, user agent BBQGrowthPlaybook/0.2 read-only-audit. A separate ad-hoc urllib request with its default user agent received HTTP 403; it is not part of the successful script sample and illustrates request-environment variation.

中文：四页实测通过的仅为脚本声明范围。四项 unknown 均为 x-default 的语言判定，属于不适用的自动判断；未将它们计为故障。sitemap 数量只是地图列出的 URL 数，不代表逐页抓取或全部收录。
